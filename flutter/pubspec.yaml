name: depass_mobile
description: GatePassX Version 2026.07.22 - mobile scanner and organizer app.
publish_to: none
version: 2026.07.22+20260722
environment:
  sdk: '>=3.7.0 <4.0.0'
  flutter: '>=3.22.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Material Design 3
  flutter_animate: ^4.5.0
  
  # Core dependencies
  cupertino_icons: ^1.0.9
  provider: ^6.1.5+1
  http: ^1.6.0
  flutter_secure_storage: ^10.3.1
  flutter_svg: ^2.3.0
  intl: ^0.20.2
  mobile_scanner: ^7.2.0
  
  # State management & utilities
  uuid: ^4.0.0
  connectivity_plus: ^6.0.0
  
  # UI/UX
  google_fonts: ^6.2.0
  shimmer: ^3.0.0
  cached_network_image: ^3.3.1
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

flutter:
  uses-material-design: true
  assets:
    - assets/icon/
